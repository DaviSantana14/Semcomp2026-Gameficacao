COMPOSE_PROJECT_NAME=semcomp
POSTGRES_DB=your_database
POSTGRES_USER=your_user
POSTGRES_PASSWORD=your_password
POSTGRES_PORT=5432

# Optional Marco 11 load rehearsal settings. Keep credentials out of the
# environment; the load script reads the administrator password from protected
# stdin and writes only aggregate metrics.
# LOAD_PARTICIPANTS=150
# LOAD_HEARTBEAT_INTERVAL_MS=60000
# LOAD_HEARTBEAT_WINDOW_MS=130000
# LOAD_REPORT_PATH=artifacts/marco-11-load-report.json
